m = User.new(name: 'mamo', email: 'mamo@exe.com', password: 'ho', password_confirmation: 'ho')

rails generate migration add_password_digest_to_users password_digest:string
rails generate migration add_index_to_users_email

rails test:models
rails test test/models/user_test.rb

foo = User.create(name: 'Foo', email: 'foo@bar.com')
User.create(name: 'A Nother', email: 'another@example.org')
user = User.new(name: 'Michael Hartl', email: 'maichael@example.com')

rails db:migrate  と rails db:rollback は、対応関係だ。
rails generate model User name:string email:string

rails generate controller Users new
rails destroy controller User new

rails test:integration
rails generate integration_test site_layout

rails generate controller Foo bar baz
rails destroy controller Foo bar baz


rails generate controller StaticPages home help

bundle _2.3.14_ config set --local without 'production'
bundle _2.3.14_ install

git remote add origin https://github.com/SakunM/sample_app.git
git push -u origin main

app/controllers/application_controller.rb

